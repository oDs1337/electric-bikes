import { createAction, props } from "@ngrx/store";

export const increment = createAction('[SHOPPING CART COMPONENT] increment');
export const decrement = createAction('[SHOPPING CART COMPONENT] decrement');
export const remove = createAction('[SHOPPING CART COMPONENT remove');
